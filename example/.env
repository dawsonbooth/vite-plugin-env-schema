# Valid examples - try changing these to invalid values to test validation!

# Must be a valid URL
VITE_API_URL=https://api.example.com

# Must be a positive integer (as string)
VITE_PORT=3000

# Must be "true" or "false" (transformed to boolean)
VITE_DEBUG=true

# Optional comma-separated list (transformed to array)
VITE_FEATURE_FLAGS=feature1,feature2,feature3